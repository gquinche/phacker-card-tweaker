streamlit>=1.58
numpy>=1.24
matplotlib>=3.9
picosvg>=0.22,<0.23
skia-pathops>=0.9.2,<0.10
Pillow>=10.0
pyyaml>=6.0
pypdf>=5.0
# Optional fallback for deployments without Chromium; the browser renderer is
# preferred when Playwright and a Chromium executable are available.
playwright>=1.50
weasyprint>=67
